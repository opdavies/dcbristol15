---
layout: default
title: Committee
---
<h1>{{ page.title }}</h1>

<ul>
  {% for person in site.committee %}
  <li>
    <h2>{{ person.name }}</h2>
    {{ person.org }}
    <a href="https://www.drupal.org/u/{{ person.drupalorg }}">{{ person.drupalorg }}</a>
    {{ person.roles }}
  </li>
  {% endfor %}
</ul>